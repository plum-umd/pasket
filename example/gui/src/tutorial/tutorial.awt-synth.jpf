target = tutorial.${xmpl}.Test
symbolic.method = tutorial.${xmpl}.Test.main(sym)

@include=../../example.jpf

@using jpf-awt-synth
#awt.script = ${config_path}/${xmpl}/${xmpl}.es

event.class = tutorial.${xmpl}.TestEvent

